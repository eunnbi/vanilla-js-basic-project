1) Math.random(): 0 이상 1 미만의 부동 소수점 난수 생성
2) Math.floor(): 소수점 버림
   Math.ceiling(): 소수점 올림
   Math.round(): 소수점 반올림
   => 정수 난수 생성을 도와줌
** min <= number <= max: Math.floor(Math.random() * (max - min + 1)) + min;

3) String(thing): 문자열의 생성자로 데이터 타입을 string으로 변환할 때 사용
   - thing: 문자열로 변환할 아무 값 (null, undefined에 대해서도 잘 동작함.)

4) Number.prototype.toString(radix): 특정 진수로 Number 객체를 나타내는 문자열 반환 (radix: 2~36 진수)
5) Object.prototype.toString(radix): 문자열을 반환하는 object의 대표적인 메서드
   - 이 메서드는 Object에서 비롯된 모든 객체에 상속됨.
   - 이 메서드가 재정의되지 않으면 "[object type]"  반환
   * toString()으로 객체 클래스 검사
   : Object.prototype.toString()을 모든 객체에 사용하게 위해 Function.prototype.call()이나 Function.prototype.apply()를 사용해여 검사하고자 하는 객체를 thisArg라 불리는 인자로 넘겨야 한다.
   
6) 문자열 연산: '+' 이어붙이기
